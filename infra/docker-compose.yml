version: '3.8'

services:
  postgres:
    image: postgres:17-alpine
    container_name: elite64-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-elite64_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-elite64_password}
      POSTGRES_DB: ${POSTGRES_DB:-elite64_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - elite64-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-elite64_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: node:18-alpine
    container_name: elite64-backend
    working_dir: /app
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-elite64_user}:${POSTGRES_PASSWORD:-elite64_password}@postgres:5432/${POSTGRES_DB:-elite64_db}?schema=public
      PORT_BACKEND: 4000
      NODE_ENV: development
      FRONTEND_URL: http://localhost:3000
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - elite64-network
    volumes:
      - ../backend:/app
      - /app/node_modules
    command: sh -c "npm install && npm run prisma:generate && npm run start:dev"

  frontend:
    image: node:18-alpine
    container_name: elite64-frontend
    working_dir: /app
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
      PORT_FRONTEND: 3000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - elite64-network
    volumes:
      - ../frontend:/app
      - /app/node_modules
      - /app/.next
    command: sh -c "npm install && npm run dev"

networks:
  elite64-network:
    driver: bridge

volumes:
  postgres_data:

