/**
 * Constantes pour les raisons de résultat de match (Phase 6.0.D)
 * 
 * Note: resultReason est un champ String? dans le schéma Prisma (pas un enum)
 * pour rester compatible avec les valeurs legacy existantes.
 */

export const RESULT_REASON_TIEBREAK_PENDING = 'TIEBREAK_PENDING' as const;

