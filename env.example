# Database
# IMPORTANT: Utilisez 'localhost' si vous exécutez Prisma depuis l'hôte Windows
# Utilisez 'postgres' uniquement si vous exécutez depuis le conteneur Docker
# NOTE: Le port est 5433 (pas 5432) car mappé dans docker-compose
DATABASE_URL=postgresql://elite64_user:Dark-Revan-GE-9418657@localhost:5433/elite64_db?schema=public

# Shadow Database (pour Prisma Migrate)
# IMPORTANT: Nécessaire pour prisma migrate dev (validation des migrations)
# Docker: utilisez le port 5433 (même port que DATABASE_URL)
# Local: utilisez le port 5432 (même port que DATABASE_URL)
# La shadow database doit être créée manuellement: CREATE DATABASE elite64_shadow;
SHADOW_DATABASE_URL=postgresql://elite64_user:Dark-Revan-GE-9418657@localhost:5433/elite64_shadow?schema=public

# Backend
PORT_BACKEND=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# JWT Authentication
# ⚠️ IMPORTANT: Changez cette valeur en production avec une clé sécurisée aléatoire
JWT_SECRET=your-secret-key-change-in-production

# SMTP Infomaniak
SMTP_HOST=mail.infomaniak.com
SMTP_PORT=587
SMTP_USER=contact@elite64.app
SMTP_PASS=U8KjUBGKG6&H7H*V
SMTP_FROM="Elite64 <no-reply@elite64.app>"

# Frontend
PORT_FRONTEND=3000
NEXT_PUBLIC_API_URL=http://localhost:4000

# PostgreSQL (pour docker-compose)
POSTGRES_USER=elite64_user
POSTGRES_PASSWORD=Dark-Revan-GE-9418657
POSTGRES_DB=elite64_db

# Admin Bootstrap
# Email du super administrateur qui sera automatiquement promu au démarrage
SUPER_ADMIN_EMAIL=florian.lantigner@ik.me